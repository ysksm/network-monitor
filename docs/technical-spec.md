# Network Monitor - 技術仕様書

## 1. アーキテクチャ概要

```
┌─────────────────┐     WebSocket / REST      ┌──────────────────┐
│                 │  ◄──────────────────────►  │                  │
│  React Frontend │                            │  Bun Backend     │
│  (Vite + TS)    │                            │  (TypeScript)    │
│                 │                            │                  │
└─────────────────┘                            ├──────────────────┤
                                               │  Scanner Service │
                                               │  - ICMP Ping     │
                                               │  - TCP Port Scan │
                                               │  Monitor Service │
                                               │  - Periodic Ping │
                                               │  - Event Log     │
                                               ├──────────────────┤
                                               │  SQLite (DB)     │
                                               └──────────────────┘
```

## 2. 技術スタック

| レイヤー | 技術 | 備考 |
|---------|------|------|
| フロントエンド | React 19 + TypeScript | Vite でビルド |
| UIライブラリ | Tailwind CSS | ユーティリティファーストCSS |
| グラフ描画 | Recharts | React向け時系列グラフ |
| バックエンド | Bun (HTTP + WebSocket) | ランタイム兼サーバー |
| DB | SQLite (bun:sqlite) | Bunネイティブサポート、ゼロ依存 |
| ping実行 | child_process (ping コマンド) | OS標準のpingを利用 |
| ポートスキャン | Bun TCP socket (net connect) | タイムアウト付きTCP接続試行 |
| DNS逆引き | dns.reverse (Node互換API) | ホスト名取得 |

## 3. ディレクトリ構成

```
network-monitor/
├── docs/
│   ├── requirements.md
│   └── technical-spec.md
├── server/                     # Bun バックエンド
│   ├── src/
│   │   ├── index.ts            # エントリポイント (HTTPサーバー + WebSocket)
│   │   ├── routes/
│   │   │   ├── scan.ts         # スキャンAPI
│   │   │   ├── hosts.ts        # ホスト管理API
│   │   │   ├── monitor.ts      # モニタリング制御API
│   │   │   └── settings.ts     # 設定API
│   │   ├── services/
│   │   │   ├── scanner.ts      # ネットワークスキャンロジック
│   │   │   ├── pinger.ts       # ping実行・応答時間計測
│   │   │   ├── port-scanner.ts # ポートスキャン
│   │   │   ├── monitor.ts      # 継続モニタリング管理
│   │   │   └── dns-resolver.ts # DNS逆引き
│   │   ├── db/
│   │   │   ├── schema.ts       # テーブル定義・マイグレーション
│   │   │   └── connection.ts   # DB接続
│   │   ├── ws/
│   │   │   └── handler.ts      # WebSocketハンドラ
│   │   └── types.ts            # 共通型定義
│   ├── package.json
│   └── tsconfig.json
├── web-frontend/               # React フロントエンド (既存)
│   ├── src/
│   │   ├── components/
│   │   │   ├── Dashboard.tsx        # メインダッシュボード
│   │   │   ├── HostTable.tsx        # ホスト一覧テーブル
│   │   │   ├── HostDetail.tsx       # ホスト詳細 + グラフ
│   │   │   ├── ScanControl.tsx      # スキャン制御パネル
│   │   │   ├── MonitorControl.tsx   # モニタリング制御
│   │   │   ├── NetworkSummary.tsx   # サマリーカード
│   │   │   ├── EventLog.tsx         # イベントログ表示
│   │   │   └── Settings.tsx         # 設定画面
│   │   ├── hooks/
│   │   │   ├── useWebSocket.ts      # WebSocket接続管理
│   │   │   ├── useHosts.ts          # ホストデータ管理
│   │   │   └── useMonitor.ts        # モニタリング状態管理
│   │   ├── types.ts                 # フロントエンド型定義
│   │   ├── api.ts                   # REST APIクライアント
│   │   ├── App.tsx
│   │   └── main.tsx
│   └── ...
└── README.md
```

## 4. データベーススキーマ

### hosts テーブル

| カラム | 型 | 説明 |
|-------|-----|------|
| id | INTEGER PRIMARY KEY | 自動採番 |
| ip_address | TEXT UNIQUE | IPアドレス |
| hostname | TEXT | ホスト名 (DNS逆引き or ユーザー設定) |
| mac_address | TEXT | MACアドレス (取得可能な場合) |
| is_online | INTEGER | 0=offline, 1=online |
| last_seen | TEXT | 最終確認日時 (ISO8601) |
| created_at | TEXT | 初回検出日時 |
| updated_at | TEXT | 更新日時 |

### open_ports テーブル

| カラム | 型 | 説明 |
|-------|-----|------|
| id | INTEGER PRIMARY KEY | 自動採番 |
| host_id | INTEGER FK | hostsへの外部キー |
| port | INTEGER | ポート番号 |
| protocol | TEXT | tcp / udp |
| service_name | TEXT | サービス名 (既知ポートの場合) |
| detected_at | TEXT | 検出日時 |

### ping_results テーブル

| カラム | 型 | 説明 |
|-------|-----|------|
| id | INTEGER PRIMARY KEY | 自動採番 |
| host_id | INTEGER FK | hostsへの外部キー |
| response_time_ms | REAL | 応答時間 (ms)、タイムアウト時はNULL |
| is_success | INTEGER | 0=失敗, 1=成功 |
| measured_at | TEXT | 計測日時 |

### events テーブル

| カラム | 型 | 説明 |
|-------|-----|------|
| id | INTEGER PRIMARY KEY | 自動採番 |
| host_id | INTEGER FK | hostsへの外部キー (NULL可) |
| event_type | TEXT | host_online, host_offline, scan_complete, threshold_exceeded |
| message | TEXT | イベントメッセージ |
| created_at | TEXT | 発生日時 |

### settings テーブル

| カラム | 型 | 説明 |
|-------|-----|------|
| key | TEXT PRIMARY KEY | 設定キー |
| value | TEXT | 設定値 (JSON) |

## 5. API設計

### REST API

#### スキャン

| Method | Path | 説明 |
|--------|------|------|
| POST | `/api/scan` | スキャン開始 (body: `{ subnet: "192.168.1.0/24", ports?: number[] }`) |
| GET | `/api/scan/status` | スキャン進捗取得 |
| DELETE | `/api/scan` | スキャン中止 |

#### ホスト

| Method | Path | 説明 |
|--------|------|------|
| GET | `/api/hosts` | ホスト一覧取得 |
| GET | `/api/hosts/:id` | ホスト詳細取得 |
| PATCH | `/api/hosts/:id` | ホスト情報更新 (hostname等) |
| DELETE | `/api/hosts/:id` | ホスト削除 |
| GET | `/api/hosts/:id/ping-history` | ping履歴取得 (query: `from`, `to`, `limit`) |

#### モニタリング

| Method | Path | 説明 |
|--------|------|------|
| POST | `/api/monitor/start` | モニタリング開始 |
| POST | `/api/monitor/stop` | モニタリング停止 |
| GET | `/api/monitor/status` | モニタリング状態取得 |

#### 設定

| Method | Path | 説明 |
|--------|------|------|
| GET | `/api/settings` | 設定取得 |
| PUT | `/api/settings` | 設定更新 |

#### イベント

| Method | Path | 説明 |
|--------|------|------|
| GET | `/api/events` | イベントログ取得 (query: `limit`, `offset`, `type`) |

### WebSocket (`/ws`)

サーバーからクライアントへリアルタイムにプッシュするメッセージ:

```typescript
// ping結果の更新
{ type: "ping_result", data: { hostId: number, ip: string, responseTime: number | null, isSuccess: boolean, measuredAt: string } }

// ホストのステータス変化
{ type: "host_status_change", data: { hostId: number, ip: string, oldStatus: "online"|"offline", newStatus: "online"|"offline" } }

// スキャン進捗
{ type: "scan_progress", data: { scanned: number, total: number, found: number } }

// スキャン完了
{ type: "scan_complete", data: { totalFound: number, duration: number } }

// アラート
{ type: "alert", data: { hostId: number, ip: string, alertType: string, message: string } }
```

## 6. 主要処理フロー

### 6.1 ネットワークスキャン

1. クライアントが `POST /api/scan` でサブネットを指定
2. サーバーがサブネットからIPアドレスリストを生成
3. 並列度を制限しつつ (同時最大50) 各IPに対して:
   a. ICMP pingを実行 (OSのpingコマンド呼び出し、1回、タイムアウト1秒)
   b. 応答があったホストに対してTCPポートスキャン実行
   c. DNS逆引きでホスト名取得試行
4. 結果をDBに保存し、WebSocketで進捗をクライアントに送信
5. スキャン完了をWebSocketで通知

### 6.2 継続モニタリング

1. クライアントが `POST /api/monitor/start` を呼び出し
2. サーバーが `setInterval` で定期処理を開始
3. 各インターバルで:
   a. 全検出済みホストに対して並列pingを実行
   b. 結果を `ping_results` テーブルに保存
   c. 前回との状態比較でオンライン/オフライン遷移を検知
   d. 遷移があれば `events` テーブルに記録
   e. WebSocketで全クライアントに結果をブロードキャスト

### 6.3 Ping実行

```typescript
// OS標準のpingコマンドを使用
// macOS: ping -c 1 -W 1000 <ip>
// Linux: ping -c 1 -W 1 <ip>
async function ping(ip: string): Promise<{ success: boolean; time: number | null }> {
  const proc = Bun.spawn(["ping", "-c", "1", "-W", "1000", ip]);
  // stdout をパースして応答時間を抽出
}
```

### 6.4 TCPポートスキャン

```typescript
async function scanPort(ip: string, port: number, timeout = 1000): Promise<boolean> {
  return new Promise((resolve) => {
    const socket = Bun.connect({
      hostname: ip,
      port,
      socket: {
        open() { socket.end(); resolve(true); },
        error() { resolve(false); },
        connectError() { resolve(false); },
      },
    });
    setTimeout(() => { socket.end(); resolve(false); }, timeout);
  });
}
```

## 7. フロントエンド画面構成

```
┌─────────────────────────────────────────────────────┐
│  Network Monitor                        [Settings]  │
├─────────────────────────────────────────────────────┤
│  ┌─────────┐ ┌─────────┐ ┌─────────────────────┐   │
│  │ Online  │ │ Offline │ │ Avg Response Time   │   │
│  │   12    │ │    3    │ │     4.2 ms          │   │
│  └─────────┘ └─────────┘ └─────────────────────┘   │
├─────────────────────────────────────────────────────┤
│  Subnet: [192.168.1.0/24  ] [Scan] [Monitor ▶]     │
├─────────────────────────────────────────────────────┤
│  IP Address    │ Hostname  │ Status │ Ping  │ Ports │
│  192.168.1.1   │ router    │ 🟢    │ 1.2ms │ 80,443│
│  192.168.1.10  │ desktop   │ 🟢    │ 0.5ms │ 22    │
│  192.168.1.20  │ printer   │ 🔴    │ -     │ -     │
│  ...           │           │        │       │       │
├─────────────────────────────────────────────────────┤
│  [Host Detail / Response Time Graph]                │
│  ┌───────────────────────────────────────────────┐  │
│  │  ╱╲    ╱╲                                     │  │
│  │ ╱  ╲╱╱  ╲    ╱╲                               │  │
│  │╱         ╲╱╱  ╲╱                              │  │
│  └───────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────┤
│  Event Log                                          │
│  [10:30:01] 192.168.1.20 went offline               │
│  [10:25:00] Scan completed: 15 hosts found          │
└─────────────────────────────────────────────────────┘
```

## 8. 開発フェーズ

### Phase 1: 基盤構築
- Bunサーバーのセットアップ (HTTP + WebSocket)
- SQLiteスキーマ作成・マイグレーション
- 基本的なREST APIエンドポイント実装

### Phase 2: スキャン機能
- ping実行サービス
- ポートスキャンサービス
- DNS逆引きサービス
- サブネットスキャンの統合

### Phase 3: モニタリング機能
- 定期ping実行
- 状態遷移検知
- イベントログ記録
- WebSocketリアルタイム通知

### Phase 4: フロントエンド
- ダッシュボードレイアウト (Tailwind CSS)
- ホスト一覧テーブル
- スキャン制御UI
- WebSocket接続・リアルタイム更新
- 応答時間グラフ (Recharts)

### Phase 5: 仕上げ
- 設定画面
- アラート通知UI
- エラーハンドリング強化
- パフォーマンス最適化

## 9. 注意事項

- ICMP pingにはroot権限が不要 (OSの`ping`コマンドを使用するため)
- ポートスキャンは自身が管理するネットワークでのみ使用すること
- 大量ホストへの同時接続を防ぐため並列度を制限する
- ping_resultsテーブルは肥大化しやすいため、古いデータの定期削除を検討する
